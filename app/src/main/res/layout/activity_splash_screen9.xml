<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/splash8Layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_gradient_purple_blue"
    android:padding="24dp">

    <!-- FrameLayout to group and shift upwards -->
    <FrameLayout
        android:id="@+id/planetBoyContainer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="60dp"
        android:layout_alignParentTop="true">

        <!-- Planets GIF -->
        <ImageView
            android:id="@+id/planetsGif"
            android:layout_width="400dp"
            android:layout_height="400dp"
            android:scaleType="fitCenter" />

        <!-- Meditating Boy -->
        <ImageView
            android:id="@+id/boyMeditating"
            android:layout_width="300dp"
            android:layout_height="300dp"
            android:layout_gravity="center"
            android:scaleType="fitCenter" />

    </FrameLayout>
    <ImageView
        android:id="@+id/nextArrow"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_marginBottom="24dp"
        android:layout_marginEnd="16dp"
        android:src="@drawable/arrow"
        android:padding="14dp" />
    <TextView
        android:id="@+id/titleText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="170dp"
        android:text="💚 Your Mind Matters Too"
        android:textColor="#FFFFFF"
        android:textSize="20sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/subText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/titleText"
        android:layout_marginTop="-170dp"
        android:layout_marginStart="24dp"
        android:gravity="start"
        android:text="Monitor your mental wellness and find your balance."
        android:textColor="#CCCCCC"
        android:textSize="16sp" />

</RelativeLayout>
